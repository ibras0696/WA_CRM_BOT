# Руководство для менеджеров и администраторов

Привет! Это короткое объяснение, как пользоваться нашим WhatsApp‑ботом, без сложных терминов.

---

## 1. Что происходит и зачем
- Бот живёт в WhatsApp. Он отвечает только тем номерам, которые вы дали администратору.
- Все действия — через обычные кнопки и короткие сообщения.
- Цель: фиксировать выдачи/возвраты денег и видеть текущий лимит каждого сотрудника.

---

## 2. Минимум подготовки
1. Администратор добавляет номер сотрудника в бота.
2. Сотрудник сохраняет контакт «CRM Bot» и пишет любое сообщение (например, «1»).
3. Всё! Дальше меню появится автоматически.

---

## 3. Общие команды
- `0` — открыть меню администратора (работает только для номеров, которым админ дал права).
- `1` — открыть меню сотрудника (если номер активирован).
- `help` / `помощь` — бот пришлёт красивую инструкцию с эмодзи и примерами.
- `отмена` — остановить текущий шаг, бот ничего не сохранит.
- На любом шаге можно снова нажать `0` или `1`, чтобы сбросить текущее состояние и вернуться к нужному меню.

---

## 4. Панель администратора (кнопка «0»)

| Кнопка | Что происходит |
|--------|----------------|
| **Добавить сотрудника** | Вводим номер в формате `7XXXXXXXXXX`. Бот активирует сотрудника. |
| **Отключить сотрудника** | Вводим номер — бот закрывает доступ. |
| **Корректировка баланса** | Вводим номер → затем сумму (`+1000` или `-500`). Лимит меняется мгновенно. |
| **Удалить операцию** | Бот показывает список сегодняшних операций. Введите ID (например, `42`). Запись помечается как удалённая. |
| **Отчёт** | Формат: `YYYY-MM-DD [YYYY-MM-DD] [номер]`. Отчёт покажет выдачи, возвраты, итоги и разрез «наличка / банк». Номер сотрудника необязателен. |
| **Отчёт за день** | Нажали кнопку — получили сводку за текущие сутки по московскому времени. |
| **Полный отчёт** | Появятся кнопки «За день / За месяц / За год / Период». Бот соберёт расширенный отчёт с оборотом, приходами/расходами и разрезом по сотрудникам. |

> Советы администраторам:
> - Если хотите выйти из любого шага (например, устали вводить даты) — отправьте `0`. Бот переключится в меню и забудет текущий запрос.
> - После добавления сотрудника не забудьте сообщить ему, что нужно нажать `1`, чтобы увидеть меню.

---

## 5. Панель сотрудника (кнопка «1»)

| Кнопка | Что делать |
|--------|------------|
| **Открыть смену** | Бот покажет остаток прошлой смены. Можно отправить `+`, чтобы принять его, или ввести новую сумму (например, `50000`). Без открытой смены операции создавать нельзя. |
| **Закрыть смену** | Когда лимиты больше не нужны — нажмите кнопку, бот закроет смену и зафиксирует остатки по наличке/банку. |
| **Новая операция** | Шаг 1: введите сумму с комментарием (`+` — пополнение, `-` — расход), например `+120000 Предоплата` или `-500 Возврат`. Шаг 2: выберите способ — «Наличка» или «Банк». После сохранения бот покажет новый баланс. |
| **Выдача рассрочки** | Введите цену товара, затем процент наценки и срок. Бот сам посчитает наценку, первый взнос, сумму рассрочки и ежемесячный платёж и уменьшит лимит на сумму выдачи. |
| **Мой баланс** | Показывает текущий лимит открытой смены. |
| **Мои операции** | Список последних операций с ID, суммой, датой и методом оплаты. После списка можно написать `#15` (или просто `15`), чтобы увидеть подробности операции, или `0`, чтобы выйти. |

> Сотрудникам:
> - Если бот пишет «Нет доступа» — значит, ваш номер ещё не активирован. Напишите администратору.
> - Сумма операции должна быть числом. `0` — не допускается.
> - После суммы бот попросит выбрать способ оплаты. Напишите `Наличка` или `Банк`, иначе операция не сохранится.
> - Используйте `+`, когда деньги пришли (баланс растёт), и `-`, когда нужно списать лимит.

---

## 6. Ежедневный ритуал
1. Утром открой смену: если бот предлагает остаток вчерашнего дня, нажми `+`, чтобы принять, или введи другое число.
2. В течение дня создавай операции `+` или `-` с комментарием и выбирай «Наличка»/«Банк».
3. Если лимит закончился — попроси администратора скорректировать баланс.
4. В 00:00 по московскому времени смена закрывается автоматически. Остаток бот предложит при следующем открытии.

---

## 7. Отчёты (для админа)
Примеры команд:
- `2025-01-01` — отчёт за один день.
- `2025-01-01 2025-01-31` — за месяц по всем сотрудникам.
- `2025-01-01 2025-01-31 79991234567` — тот же период, но только по одному сотруднику.

Отчёт показывает:
```
📊 Отчёт 01.01.2025 — 31.01.2025
Всего операций: 12
💸 Выдачи: 1 200 000.00 (шт. 8)
↩️ Возвраты: 150 000.00 (шт. 3)
🧮 Итог: 1 050 000.00
Наличка: 700 000.00 (шт. 5)
Банк: 350 000.00 (шт. 3)

👥 По сотрудникам:
• Иван — выдачи 600 000.00 / возвраты 50 000.00 / итог 550 000.00 / наличка 400 000.00 / банк 150 000.00
```

Нужно посмотреть только текущий день — нажмите кнопку «Отчёт за день», и бот пришлёт такую же сводку за последние сутки.

---

## 8. Частые вопросы

**Бот молчит** — отправьте `0` или `1`. Если всё равно тишина, ваш номер не добавлен. \
**Забыл, какие кнопки есть** — напишите `help`, и бот пришлёт большую инструкцию с эмодзи. \
**Нужно срочно выйти из сценария** — напишите `0` или `отмена`. \
**Лимит ушёл в минус** — попросите администратора нажать «Корректировка баланса». \
**Операция создана ошибочно** — администратор нажимает «Удалить операцию» и вводит ID. \
**Хочу посмотреть детали своей операции** — откройте «Мои операции» и после списка введите `#ID` (например, `#42`).

---

## 9. Контакты и поддержка
- **Разработчик**: команда CRM WA Bot.
- **Как получить помощь**: напишите администратору или передайте ошибку разработчику (желательно с текстом, который бот вам прислал).

Спасибо, что пользуетесь ботом! Если есть идеи по улучшению — смело делитесь. 🙌
